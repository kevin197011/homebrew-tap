class <%= @app_class_name %> < Formula
  desc "<%= @app_name %> command tool"
  homepage "https://github.com/kevin197011"
  version "<%= @app_version %>"
  url "https://github.com/kevin197011/utils/raw/main/<%= @app_name %>_#{version}.tar.gz"
  sha256 "<%= @app_sha256 %>"
  license "MIT"


  def install
    bin.install "<%= @app_name %>"
  end

  test do
    system "false"
  end
end
